services:
  proxy:
    image: nginx:alpine
    volumes:
      - ./fastapi/app:/var/www/html/api
      - ./nginx/conf.d:/etc/nginx/conf.d
    ports:
      - "80:80"
    networks:
      - app_network

  django:
    build:
      context: ./django
    volumes:
      - ./Prover/app:/app  # Volume para os arquivos da aplicação Django
    # command: >
    #   sh -c "python manage.py migrate &&
    #          python manage.py runserver"
    networks:
      - app_network

  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: app_db
      MYSQL_USER: app_user
      MYSQL_PASSWORD: app_password
    volumes:
      - ./mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
      - mysql_data:/var/lib/mysql
    networks:
      - app_network

networks:
  app_network:
    driver: bridge

volumes:
  mysql_data: